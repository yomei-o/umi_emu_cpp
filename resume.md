# resume.md — 次に触るときの覚書

最終更新: 2026-08-17

## いまどうなっているか

3次元の水のシミュレータ。[webgpu-ocean](https://github.com/matsuoka-601/webgpu-ocean) と同じ骨格を
**GPU を使わず C++ だけで**。MLS-MPM ＋ 画面空間の流体レンダリング。動いている。

- GitHub: https://github.com/yomei-o/umi_emu_cpp （main に push 済み）
- デモ: https://yomei-o.github.io/umi_emu_cpp/wasmdist/umi_os/
- 参考画像（合わせにいった写真）: `6b4cd02bf88e19e12f3d39e3456b558386f22796-thumb-1920x1080-64927.jpg`

## すぐ動かす

```sh
cd /c/prog/claude/umi_emu_cpp

# WASM ビルド
EMSDK=/c/prog/emsdk/emsdk ./build.sh umi_os

# ブラウザ
cd wasmdist && python -m http.server 8765     # -> http://localhost:8765/umi_os/

# ネイティブ自己テスト（PNG を書き出す。毎段の所要時間も出る。目視確認はこれが速い）
clang++ -O3 -std=c++17 -I. -o umi_o3.exe src/umi_os.cpp
./umi_o3.exe 700 out.png                       # 引数: フレーム数 出力PNG
LEVEL=4 ./umi_o3.exe 900 out.png               # 粒子数プリセット 0..5
PERCELL=3 SUB=3 ./umi_o3.exe 700 out.png       # 1セルあたり粒子数 / サブステップ
```

## ファイル

| ファイル | 中身 |
|---|---|
| `src/umi_os.cpp` | 物理・描画すべて（これ1本。約810行） |
| `build.sh` | em++ ラッパ（`-msimd128` 付き） |
| `wasmdist/umi_os/index.html` | JSハーネス（hanabi / taki と同じ ABI） |
| `olive.c` / `stb_image_write.h` | HUD文字 / ネイティブPNG出力 |

## ABI（sim_set の id）

| id | 意味 | 既定 |
|---|---|---|
| 0 | 体積弾性率 E（変えると dt を引き直す） | 400 |
| 1 | 重力 | 9.8 |
| 2 | サブステップ | 2 |
| 3/4/5 | 視点 方位[°] / 仰角[°] / 距離 | 19.5 / 7.4 / 2.30 |
| 6 | 球の半径（dx 単位） | 0.88 |
| 7 | density（吸収のスケール） | 1.5 |
| 8 | 表面の平滑化 | 2.0 |
| 9 | 表示モード 0水 1法線 2深度 3厚み 4粒子 | 0 |
| 10 | 粒子数プリセット 0..5 | 1 |
| 11 | 1セル1辺あたりの粒子数 1..3 | 2 |
| 13 | HUD 0/1 | 1 |

`sim_get`: 0=粒子数 1=フレーム 2=レベル 3=PERCELL 4/5/6=GX/GY/GZ

## 粒子数プリセット（ネイティブ -O3 実測）

水槽の実寸（2.0 × 1.0 × 1.3 m）は固定で、格子の細かさだけ変える。

| lv | 格子 | 粒子数 | ms/frame | 換算 |
|---|---|---|---|---|
| 1 | 40×20×26 | 37,989 | 42 | 24 fps |
| 2 | 48×24×31 | 70,356 | 58 | 17 fps |
| 3 | 56×28×36 | 118,482 | 82 | 12 fps |
| 4 | 64×32×42 | 188,784 | 113 | 9 fps |
| 5 | 76×38×49 | 328,735 | 172 | 6 fps |

lv4 以上は本家（10万〜30万）と同じ規模まで出る。**WASM はこれより遅い**ので実測すること。

※ 格子を細かくすると dt も小さくなる（CFL）ので、**同じサブステップ数だと時間の進みも遅くなる**。

---

# 課題（ここから続き）

## 1. 【最優先】水面のギラつきを消す

いま一番の画質差。本家より白い筋が多い。原因は**法線のノイズ**で、元は2つ。

- 深度バッファが**半解像度**（`RS=2`）。法線は深度の差分なので、半分の格子で微分すると荒い。
- 格子が粗い（lv1 で dx=1/20）。

**やること**: 深度パスだけ全解像度で撒き、平滑化と着色は半解像度のまま、という分離を試す。
あるいは平滑化のあと 3×3 で法線専用のもう一段ぼかしを入れる。
`p_mode=1`（法線表示）にすると、ノイズがそのまま見えるので判定しやすい。

## 2. 速度

lv1 で 42 ms/frame（ネイティブ -O3）。内訳（構図によって変動する）:

```
MPM 15.1 ms   depth 14.9 ms   filter 4.8 ms   shade 6.6 ms
```

- **depth が画面占有率に比例する**。カメラを寄せると倍近くなる。
  → 球の描画をタイル分割して、遠い粒子はスプライトを小さく打ち切る等
- **MPM は SIMD 化していない**。`-msimd128` は付けたが手で書いてはいない。
  P2G/G2P の内側 3ループは 4 幅にまとめられるはず。ここが一番伸びしろ
- 粒子のセル順ソートは入れたが、初期配置がすでにセル順なので**短い走行では効果ゼロ**だった。
  長く回したときに効くはずなので、10000フレーム走らせて計測し直すこと

## 3. まだやっていないこと

- **飛沫シーンがない**。本家の写真は中央に大きな飛沫が上がっている。
  いまはダムブレイクが崩れるだけ。「水塊を落とす」「壁を動かす」アクションが欲しい
- **壁の可視化がない**。水が宙に浮いて見える。うっすら箱の輪郭か床の影があると据わりが良い
- マウスで視点を回せない（スライダのみ）
- `sim_click` が空。本家のように水をかき混ぜたい

## 4. スレッド

WASM の pthreads は **GitHub Pages では使えない**（COOP/COEP ヘッダが送れず SharedArrayBuffer が無効）。
自前ホストか Cloudflare Pages 等に置けば 4 コアで 3〜4 倍は狙える。判断が要る。

---

# 踏んだ罠（同じ失敗を繰り返さないため）

## 物理

- **dt を固定値にすると必ず飛ぶ。** 弾性波の速さ c=√(E/ρ) に対して dt < dx/c。
  最初 dt=1.5e-3 で J が NaN になった。いまは `retune()` で `dt = 0.35·dx/c`。
  **E を変えたら dt を引き直すこと**（`sim_set(0,...)` は中でやっている）
- **P_VOL は粒子の間隔の3乗**。1セルあたりの粒子数を変えたのに P_VOL を固定にすると
  密度が変わって水でなくなる。`P_VOL = (dx/PERCELL)³`

## 描画（★本家のソースを読んで直したところ。ここが一番大事）

先に自分で書いてから `render/fluid.wgsl` を読んだら、要所で間違えていた。

| | 自分の実装 | 本家 | 症状 |
|---|---|---|---|
| 消散係数 | (6.2, 2.3, 0.7) | **(1.37, 0.54, 0.15)** | 厚いところが**黒**になっていた |
| 屈折 | 背景を法線ぶんずらす | **ずらさない**。平らな灰 0.8 に透過率を掛けるだけ | 余計な歪み |
| 出力 | `x/(1+x)` トーンマップ＋ガンマ0.85 | **素通しでクランプのみ** | 全体が白っぽく眠かった |
| 厚み | 実際の光路長[m]に較正 | `0.05·√(1−r²)` の無次元和 | 係数を合わせるなら本家式 |
| 反射 | 灰色の空 | **青空 cubemap**（上(0.35,0.56,0.74) 下(0.24,0.35,0.49)） | 照り返しが灰色すぎた |

本家の式（`render/fluid.wgsl`）:

```
bgColor       = 0.8
diffuseColor  = (0.085, 0.6375, 0.9)
transmittance = exp(-1.5 · thickness · (1 - diffuseColor))
refraction    = bgColor · transmittance
fresnel       = 0.02 + 0.98 · (1 - dot(n, -rayDir))^5
specular      = pow(dot(H, n), 250),  H = normalize(lightDir - rayDir), lightDir = ワールドの (0,0,-1)
final         = specular + mix(refraction, reflection, fresnel)
```

**青は赤の 1/9 しか吸われない。だから厚くなっても黒ではなく濃い群青で止まる。**
水の青は色を塗ったものではない。

本家の箱とカメラ（`main.ts`）:

```
mlsmpmInitBoxSizes = [[35,25,55], [40,30,60], [45,40,80], [50,50,80]]   ← [1] を使用
mlsmpmNumParticleParams = [40000, 70000, 120000, 200000]
mlsmpmInitDistances = [60, 70, 90, 100]      fov = 45°, radius = 0.6 セル
camera target = (W/2, H/4, D/2)
initDambreak: x は端から3セル、y は 0.8H まで、z は奥行きの半分まで
```

## 実装

- **推測せずに毎段の時間を測ること。** 素直に書いて 145 ms/frame だったとき、
  内訳を出したら **depth が 93.9 ms** で他を圧倒していた（球1個が835画素塗っていた）。
  そこだけ潰して 18倍。`tSim/tDepth/tFilt/tShade` を出す仕組みが入っている
- 効いた順: ①流体だけ半解像度 ②**内部の粒子を描かない**（質量格子で6近傍が詰まっていたら捨てる）
  ③背景を全解像度で焼いて水のある矩形だけ処理 ④expf/powf を表引き
- **矩形だけ処理すると継ぎ目が出る。** 矩形の内と外でガンマの掛け方が違って枠が見えた。
  拡大が双一次で1画素はみ出して読むので、着色は矩形より3画素広く塗る
- Windows の bash ヒアドキュメントで python を書くとき、C 文字列の中の `\n` が
  **本物の改行に潰れて** printf が壊れる。`\n` を含むパッチは Edit ツールを使うこと（4回踏んだ）
